<p>Add an interest</p>
<ul>
  <%= form_for [@user, @interest], remote: true do |f| %>
    <li><%= f.label :name, 'Interest' %>
      <%= f.text_field :name%></li>
      <li><%= f.submit %></li>
  <% end %>
</ul>
