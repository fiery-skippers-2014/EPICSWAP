<div id="container">
  <h1><%= @interest.name.capitalize %></h1>
  <ul>
    <% @sorted_users.each do |user, distance| %>
<!--       you can move this to a view's helper method -->
      <li><%= link_to user.name, user_path(user) %> - <%= distance %> miles away</li>
    <% end %>
  </ul>
</div>
