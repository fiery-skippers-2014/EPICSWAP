<div id="container">

  <h2>All Skills</h2>
  <ul>
    <% @skills.each do |skill| %>
      <li><%= link_to skill.name, skill_path(skill) %> <%= pluralize(skill.users.count, 'teacher') %></li>
    <% end %>
  </ul>
</div>
